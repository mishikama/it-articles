apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - image-update.yaml
configMapGenerator:
  - name: app-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
